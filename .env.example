# ============================================================
# VARIABLES DE ENTORNO - EJEMPLO PARA RAILWAY
# ============================================================
# Copia este archivo a .env y llena los valores correspondientes
# NUNCA subes .env a Git - solo .env.example

# Flask Configuration
FLASK_ENV=production
SECRET_KEY=your-secret-key-here-min-32-characters
CSRF_SECRET_KEY=your-csrf-secret-key-here-min-32-characters

# Base de Datos (Local Development)
# En Railway, DATABASE_URL se genera automáticamente
DB_HOST=localhost
DB_PORT=5432
DB_NAME=app_juzgado
DB_USER=postgres
DB_PASSWORD=your-password-here

# En Railway (producción), se genera automáticamente:
# DATABASE_URL=postgresql://user:password@host:port/database

# Puerto (Railway asigna automáticamente)
PORT=5000

# Workers para Gunicorn
WEB_CONCURRENCY=2
